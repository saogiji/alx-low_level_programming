#include <stdio.h>

/**
 * main - prints the lower case of the alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char alpha
	
	for (char = 'a'; alpha <= 'z'; x++)
	{
		 putchar(alpha);
	}
	putchar('\n');
return (0);
}
